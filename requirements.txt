# --- Ядро приложения -------------------------------------------------------
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.6
sqlalchemy[asyncio]>=2.0.25
aiosqlite>=0.19
python-multipart>=0.0.9

# --- Десктопная оболочка ---------------------------------------------------
pywebview>=4.4
# На Windows PyWebView рендерит через WebView2 и требует моста в .NET.
# Маркер не даёт pip тянуть pythonnet на других платформах.
pythonnet>=3.0.3 ; sys_platform == "win32"

# Значок в трее и всплывающие уведомления о дедлайнах.
# Без них приложение работает, но закрытие окна завершает процесс,
# и напоминания перестают приходить.
pystray>=0.19.5
pillow>=10.0

# --- Локальная модель (опционально) ----------------------------------------
# llama-cpp-python вынесен в requirements-llm.txt: он собирается из исходников
# и требует MSVC Build Tools + CMake. Если поставить его в общий список,
# падение компиляции заблокирует установку всего остального.
#   pip install -r requirements-llm.txt

# --- Сборка .exe -----------------------------------------------------------
# Ставятся отдельно в build_windows.ps1, здесь для справки:
#   pyinstaller>=6.3
#   pillow>=10.0          (только для tools/make_icon.py)
