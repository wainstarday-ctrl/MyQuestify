Overdue quests were only noticed when the app started or came back from the background. If the app stayed open, the late penalty was not applied until a restart. The check now also runs on a timer.

The quest card updates right after the penalty is applied — previously it kept its old state on screen.
